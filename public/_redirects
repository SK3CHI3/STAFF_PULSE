# Netlify redirects for Next.js App Router
# Handle client-side routing - this is CRITICAL for SPA routing

# All other routes should fallback to index.html for client-side routing
# API routes are automatically handled by Netlify's Next.js integration
/* /index.html 200
